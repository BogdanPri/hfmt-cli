# utils

A collection of miscellaneous utility tools and CLI apps — lightweight scripts designed to make everyday tasks faster and easier.

## 📦 Overview

This repository is a growing toolbox of small, focused utilities. Each utility can be used independently. The goal is simplicity, portability, and reusability.

Current utilities include:

- **header-cli** — Generate formatted text headers for scripts, logs, or documentation.

## 🚀 Installation

Clone the repository:

```bash
git clone https://github.com/your-username/utils.git
cd utils
```

Make files executable:

```bash
chmod +x header-cli
```

Additionaly, you can copy any app to a location in your PATH:

```bash
cp header-cli ~/.local/bin/header-cli
```
